# Copy this to .env for local (non-docker) development.
# In Docker Compose / Kubernetes these are supplied as env vars instead.
PORT=8081
PGHOST=localhost
PGPORT=5432
PGUSER=petapp
PGPASSWORD=petapp
PGDATABASE=petapp
